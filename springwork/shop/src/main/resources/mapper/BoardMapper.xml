<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.multi.shop.mapper.BoardMapper">

    <select id="findAll" resultType="com.multi.shop.domain.Board">
        SELECT * FROM board ORDER BY created_at DESC
    </select>

    <insert id="save">
        INSERT INTO board (title, content, writer)
        VALUES (#{title}, #{content}, #{writer})
    </insert>

    <select id="findById" parameterType="int" resultType="com.multi.shop.domain.Board">
        SELECT * FROM board WHERE id = #{id}
    </select>

    <update id="update">
        UPDATE board
        SET title = #{title}, content = #{content}, writer = #{writer}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM board WHERE id = #{id}
    </delete>
</mapper>
